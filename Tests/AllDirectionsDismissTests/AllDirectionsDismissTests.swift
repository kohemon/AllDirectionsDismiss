import XCTest
@testable import AllDirectionsDismiss

final class AllDirectionsDismissTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(AllDirectionsDismiss().text, "Hello, World!")
    }
}
